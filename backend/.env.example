# Copie este arquivo para .env e preencha com seus valores.
# NUNCA suba o .env para o Git.

PORT=3001
NODE_ENV=development
BASE_URL=http://localhost:3001

JWT_SECRET=troque_por_uma_string_longa_e_aleatoria
JWT_EXPIRES_IN=24h

# Pagar.me — obtenha em https://dashboard.pagar.me
# Sandbox: prefixo sk_test / pk_test
# Producao: prefixo sk_live / pk_live
PAGARME_SECRET_KEY=sk_test_COLE_SUA_CHAVE_AQUI
PAGARME_PUBLIC_KEY=pk_test_COLE_SUA_CHAVE_AQUI

# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
PAGARME_WEBHOOK_SECRET=gere_uma_string_aleatoria_aqui

# Cloudinary — obtenha em https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=seu_cloud_name
CLOUDINARY_API_KEY=sua_api_key
CLOUDINARY_API_SECRET=seu_api_secret

# Email (opcional)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=seu_email@example.com
SMTP_PASSWORD=sua_senha_email
